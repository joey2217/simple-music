import { describe, expect, test } from 'vitest'
import netease from '../src/renderer/main/musicResource/netease'

describe('测试网易云API', () => {
  // test('fetchSearchResult result length > 0', async () => {
  //   const res = await netease.fetchSearchResult({keyword:'你好'})
  //   // console.log(res[0])
  //   console.log(res);
  //   // expect(res.length).greaterThan(0)
  //   expect(res).not.toBeNull()
  // })
  // test('fetchHotSearch result length > 0', async () => {
  //   const res = await netease.fetchHotSearch()
  //   // console.log(res[0])
  //   console.log(res);
  //   // expect(res.length).greaterThan(0)
  //   expect(res).not.toBeNull()
  // })
  // test('fetchTopList result length > 0', async () => {
  //   const res = await netease.fetchTopList()
  //   // console.log(res[0])
  //   expect(res.length).greaterThan(0)
  // })
  // test('fetchTopListDetail result not null', async () => {
  //   const id = 19723756
  //   const songListDetail = await netease.fetchTopListDetail(id)
  //   expect(songListDetail).not.toBeNull()
  // })
  // // test('fetchSongListData result length > 0', async () => {
  // //   const id = 2829733864
  // //   const songs = await netease.fetchSongListData([id])
  // //   console.log(songs)
  // //   expect(songs.length).greaterThan(0)
  // // })
  // test('fetchHotSearch not null', async () => {
  //   const res = await netease.fetchHotSearch()
  //   console.log(res)
  //   expect(res).not.toBeNull()
  // })
})
